/**
 * How long the tamed creature is in the air on its way to the glade.
 *
 * Its own module so the card that confirms the tame can hold its badge and
 * sparkles until the flight has actually landed, without either file
 * restating the number and the two quietly drifting apart. The tame reads as
 * one sequence only while they agree.
 */
export const FLIGHT_MS = 900;
