{
  "permissions": {
    "allow": [
      "Bash(pnpm tsc --noEmit *)",
      "Bash(pnpm exec tsc --noEmit *)",
      "Bash(pnpm lint *)",
      "Bash(pnpm test --run *)",
      "Bash(pnpm vitest run *)",
      "Bash(pnpm exec biome check --error-on-warnings *)",
      "Bash(pnpm exec tsx scripts/snapshot-grid.ts *)",
      "Bash(npx tsc --noEmit *)"
    ]
  },
  "hooks": {
    "WorktreeCreate": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import sys,json,subprocess; d=json.load(sys.stdin); p=d['cwd']+'/.claude/worktrees/'+d['name']; subprocess.run(['pnpm','i'],cwd=p)\" || true",
            "statusMessage": "Installing dependencies in worktree..."
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "[ -f .port ] && PORT=$(cat .port) && PID=$(ss -tlnp \"sport = :$PORT\" 2>/dev/null | grep -oP 'pid=\\K[0-9]+' | head -1) && [ -n \"$PID\" ] && kill -- -$(ps -o pgid= -p $PID 2>/dev/null | tr -d ' ') 2>/dev/null || true"
          }
        ]
      }
    ]
  },
  "enabledPlugins": {
    "playwright-skill@playwright-skill": true,
    "frontend-design@claude-plugins-official": true
  }
}
