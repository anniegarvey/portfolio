---
name: playwright-skill
description: Complete browser automation with Playwright. Auto-detects dev server via .port file. Test pages, fill forms, take screenshots, check responsive design, validate UX, automate any browser task. Use when user wants to test websites, automate browser interactions, validate web functionality, or perform any browser-based testing.
---

# Playwright Browser Automation

Choose suitable playwright usage type:

- **Writing a test spec** (permanent e2e coverage, or a scripted repeatable ad-hoc script e.g. a multi-viewport screenshot sweep) → `./test-specs.md`
- **Interactive control from chat** (poke the running app, inspect a11y roles, click through a flow once, iterate live on a design, no file written) → `./mcp.md`
  - Not configured yet (no `playwright` entry in `.mcp.json`)? → `./mcp-setup.md` first
